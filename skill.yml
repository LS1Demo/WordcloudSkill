# skill identifier
owner: "leftshiftone"
name: "wordcloud"

# additional properties
labels: ["wordcloud", "semantic"]
authors: ["devs@leftshift.one"]

language: "python-3.7"

resources:
  cpu:
    min: 125
  memory:
    min: 125

contract: ["./wordcloud"]

# the main class to execute
handler:
  file: "handler"
  function: "evaluate"
